# Diagnostics

The rules for this folder — `LogScrubber` as the single scrubbing SSOT, the diagnostics export, server uploads through `DiagnosticsUploader`, MetricKit retention — live in **`Shared/Screens/Settings/CLAUDE.md`**, under the Debug section. Read them before editing anything here.
