# Archive

Documents kept for history only. Nothing here is current, and nothing here is
authoritative — the code and the `CLAUDE.md` files are.

| Path | What it was | Why archived |
|---|---|---|
| `superpowers/` | Design specs and implementation plans (2026-07 → 2026-09) | Every feature they describe has shipped; the code and its RULEs supersede them |
| `architecture/` | Long-form copies of CLAUDE.md sections (admin, playback) | Stale copies — they had drifted from CLAUDE.md (« Settings → Interface → Debug », `URLSession.shared` for artwork) |
| `v2-todo.md` | Items pulled out of the v1 App Store submission | Its only item shipped in 2.0.0 (`ProfileScreen`) |
| `testing/` | Test plan of the perf branch (PR #109, 2026-08) | That branch is merged; its checks are now unit tests or RULEs |

Archived by the 2026-09-22 audit, lot 5 (`docs/audits/2026-09-22-full-audit.md`).
